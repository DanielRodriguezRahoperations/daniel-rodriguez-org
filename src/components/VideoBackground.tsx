// Video is now rendered inside Hero.tsx as a self-contained sticky hero.
// This component is retained as a stub so App.tsx does not need modification.
export default function VideoBackground() {
  return null
}
